export const initInputMessage = `Check out this text formatter!

Insert variables by wrapping them {LIKE_THIS}

Insert multi-select variables by adding & between options {LIKE&THIS}

Insert the current !DATE <- like that.

Now go save yourself some time.
`;
